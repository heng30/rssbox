export struct Translation {
    new: string,
    edit: string,
    delete: string,
    cancel: string,
    ok: string,
    setting: string,
    about: string,
    help: string,
    setting-btn: string,
    about-btn: string,
    help-btn: string,
    name: string,
    warning: string,
    delete-or-not: string,
    reset-or-not: string,

    setting-dialog-ui: string,
    setting-dialog-ui-font-size: string,
    setting-dialog-ui-font-family: string,
    setting-dialog-ui-win-WH: string,
    setting-dialog-ui-win-W: string,
    setting-dialog-ui-win-H: string,
    setting-dialog-ui-language: string,

    setting-dialog-rss: string,
    setting-dialog-rss-sync-interval: string,
    setting-dialog-rss-sync-timeout: string,
    setting-dialog-rss-sync-browser: string,
    setting-dialog-rss-sync-browser-placeholder: string,
    setting-dialog-rss-start-sync-enabled: string,
    setting-dialog-rss-start-sync-disabled: string,

    setting-dialog-proxy: string,
    setting-dialog-proxy-type: string,
    setting-dialog-proxy-url: string,
    setting-dialog-proxy-url-placeholder: string,
    setting-dialog-proxy-port: string,
    setting-dialog-proxy-port-placeholder: string,

    rss-unread: string,
    rss-favorite: string,

    rss-dialog-title-new: string,
    rss-dialog-title-edit: string,
    rss-dialog-name: string,
    rss-dialog-name-placeholder: string,
    rss-dialog-url: string,
    rss-dialog-url-placeholder: string,
    rss-dialog-unused-proxy: string,
    rss-dialog-used-proxy: string,
    rss-dialog-feed-format: string,

    icon-dialog-title: string,

    no-data: string,
    no-message: string,

    tip-copy: string,
    tip-copy-url: string,
    tip-delete: string,
    tip-delete-all: string,
    tip-send: string,
    tip-clear: string,
    tip-stop: string,
    tip-reset: string,
    tip-save: string,
    tip-help: string,
    tip-about: string,
    tip-setting: string,
    tip-edit: string,
    tip-up: string,
    tip-down: string,
    tip-show: string,
    tip-hide: string,
    tip-select-icon: string,
    tip-sync-rss: string,
    tip-favorite: string,
    tip-mark-read: string,
    tip-set-all-read: string,
    tip-is-read: string,
    tip-is-not-read: string,
}

export global Translator {
    public pure function translation(is-cn: bool) -> Translation {
        return {
            new: is-cn ? "新建" : "New",
            edit: is-cn ? "编辑" : "Edit",
            delete: is-cn ? "删除" : "Delete",
            cancel: is-cn ? "取消" : "Cancel",
            ok: is-cn ? "确定" : "Confirm",
            setting-btn: is-cn ? "设置" : "Setting",
            about-btn: is-cn ? "关于" : "About",
            help-btn: is-cn ? "帮助" : "Help",
            setting: is-cn ? "设     置" : "Setting",
            about: is-cn ?   "关     于" : "About",
            help: is-cn ?    "帮     助" : "Help",
            name: is-cn ?    "名     称" : "Name",
            warning: is-cn ? "警     告" : "Warning",
            delete-or-not: is-cn ? "是否删除？" : "Are You Sure to Delete?",
            reset-or-not: is-cn ? "是否重置？" : "Reset or not?",

            setting-dialog-ui: is-cn ? "界面" : "UI",
            setting-dialog-ui-font-size: is-cn ? "字体大小:" : "Font Size:",
            setting-dialog-ui-font-family: is-cn ? "字体样式:" : "Font Family:",
            setting-dialog-ui-win-WH: is-cn ? "窗口大小:" : "Window Size:",
            setting-dialog-ui-win-W: is-cn ? "宽" : "Width",
            setting-dialog-ui-win-H: is-cn ?  "高" : "Height",
            setting-dialog-ui-language: is-cn ? "选择语言:" : "Language:",

            setting-dialog-rss: is-cn ? "RSS" : "RSS",
            setting-dialog-rss-sync-interval: is-cn ? "更新间隔(min):" : "Sync Interval(min):",
            setting-dialog-rss-sync-timeout: is-cn ? "更新超时(sec):" : "Sync Timeout(sec):",
            setting-dialog-rss-sync-browser: is-cn ? "浏览器:" : "Browser:",
            setting-dialog-rss-sync-browser-placeholder: is-cn ? "请输入浏览器": "Please Set Browser",
            setting-dialog-rss-start-sync-enabled: is-cn ? "启用启动同步" : "Enabled Sync When Startup",
            setting-dialog-rss-start-sync-disabled: is-cn ? "未启用启动同步" : "Disabled Sync When Startup",

            setting-dialog-proxy: is-cn ? "代理" : "Proxy",
            setting-dialog-proxy-type: is-cn ? "代理类型:" : "Type:",
            setting-dialog-proxy-url-placeholder: is-cn ? "请输入URL/IP:" : "URL/IP",
            setting-dialog-proxy-port: is-cn ? "端口:" : "Port:",
            setting-dialog-proxy-port-placeholder: is-cn ? "请输入端口" : "Port",

            rss-unread: is-cn ? "未读消息" : "Unread Message",
            rss-favorite: is-cn ? "收藏消息" : "Favorite Message",

            rss-dialog-title-new: is-cn ?  "新    建" : "New",
            rss-dialog-title-edit: is-cn ? "编    辑" : "Edit",
            rss-dialog-name: is-cn ? "名        称:" : "Name:",
            rss-dialog-name-placeholder: is-cn ? "请输入RSS名称" : "Please Input RSS Name",
            rss-dialog-url: is-cn ? "RSS 地址:" : "RSS Address:",
            rss-dialog-url-placeholder: is-cn ? "请输入RSS地址" : "Please Input RSS Address",
            rss-dialog-unused-proxy: is-cn ? "未启用代理" : "Disabled Proxy",
            rss-dialog-used-proxy: is-cn ? "已启用代理" : "Enabled Proxy",
            rss-dialog-feed-format: is-cn ? "协议类型:" : "Protocol Type:",

            icon-dialog-title: is-cn ? "图标库" : "Icons",

            no-data: is-cn ? "没有数据" : "No Data",
            no-message: is-cn ? "没有信息" : "No Message",

            tip-copy: is-cn ? "复制" : "Copy",
            tip-copy-url: is-cn ? "复制网址" : "Copy URL",
            tip-send: is-cn ? "发送" : "Send",
            tip-clear: is-cn ? "清除" : "Clear",
            tip-stop: is-cn ? "停止" : "Stop",
            tip-reset: is-cn ? "重置" : "Rest",
            tip-delete: is-cn ? "删除" : "Delete",
            tip-delete-all: is-cn ? "删除所有" : "Delete All",
            tip-save: is-cn ? "保存" : "Save",
            tip-help: is-cn ? "帮助" : "Help",
            tip-about: is-cn ? "关于" : "About",
            tip-setting: is-cn ? "设置" : "Setting",
            tip-edit: is-cn ? "编辑" : "Edit",
            tip-up: is-cn ? "向上" : "Move Up",
            tip-down: is-cn ? "向下" : "Move down",
            tip-show: is-cn ? "显示" : "Show",
            tip-hide: is-cn ? "隐藏" : "Hide",
            tip-select-icon: is-cn ? "选择图标" : "Choose Icon",
            tip-sync-rss: is-cn ? "同步RSS" : "Sync RSS",
            tip-favorite: is-cn ? "收藏" : "Favorite",
            tip-mark-read: is-cn ? "标记为已读" : "Mark Read",
            tip-set-all-read: is-cn ? "全部标记为已读" : "Set All Entry Read",
            tip-is-read: is-cn ? "已读" : "Read",
            tip-is-not-read: is-cn ? "已读" : "Read",
        };
    }
}
